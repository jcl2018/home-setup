# /project:deploy — Deploy Repo to ~/.claude/

Run `bash scripts/deploy.sh` to sync the repo to `~/.claude/`.

Supports `--dry-run` to preview changes without executing.

After deploy, suggest running `/project:audit` to verify.
