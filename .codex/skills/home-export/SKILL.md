---
name: home-export
description: Export the active Codex home layout into the local mirror repo. Use when the user wants a snapshot or backup of the current Codex home control layer.
---

# Home Export

Run `~/.codex/bin/codex-home-export --repo ~/Documents/projects/home-setup` unless the user names a different local mirror path.

Use this to snapshot the active Codex home layout only. Do not export `~/.claude`.
